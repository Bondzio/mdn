	<div>
		<div id="top-nav"></div>
        <div id="logo"><%= image_tag("logos/mdn_logo_wh.png", :width =>'276', :height =>'90', :alt => "Men's Discipleship Network Logo")%></div>
        <div id="mdn_bookstore"><a href="http://mdn.christianbook.com" target="self"><%= image_tag("promos/mdn_bookstore.png", :alt => 'MDN Bookstore') %></a></div>
        <div id="header_countdown" class="center"><p>DBC Begins: <span id="event_countdown"></span></p></div>
        <div id="white_bar"></div>
        <div id="lower-nav"></div>
        <!-- <div id="donate-badge"> <a href="/pages/donate"><img src="/images/donate/donate-badge.gif"> </a></div> -->
        <!-- Once all the logic is complete, remove this comment to activate user login links -->
        
		<%# render "/shared/login" %>
    </div> <!-- end of header -->
    
<!-- NAVIGATION LINKS GO HERE -->
     <%= render "/shared/menu"	 %>

<script type="text/javascript">
    // set the date we're counting down to
    var target_date = new Date("May 30, 2015").getTime();
     
    // variables for time units
    var days, hours, minutes, seconds;
     
    // get tag element
    var event_countdown = document.getElementById("event_countdown");
     
    // update the tag with id "countdown" every 1 second
    setInterval(function () {
     
        // find the amount of "seconds" between now and target
        var current_date = new Date().getTime();
        var seconds_left = (target_date - current_date) / 1000;
     
        // do some time calculations
        days = parseInt(seconds_left / 86400);
        seconds_left = seconds_left % 86400;
         
        hours = parseInt(seconds_left / 3600);
        seconds_left = seconds_left % 3600;
         
        minutes = parseInt(seconds_left / 60);
        seconds = parseInt(seconds_left % 60);
         
        // format countdown string + set tag value
        event_countdown.innerHTML = days + "days, " + hours + "hours, "
        + minutes + "m, " + seconds + "s";  
     
    }, 1000);

</script>