<h1>Events List</h1></br>
<p>This is the list of events that appear on the events web page. You may add new events, edit existing ones, or destroy(delete) existing ones.</p></br>

<table >
  <tr>
    <th>Title</th>
    <th>Date</th>
    <th>Time</th>
    <th>Venue</th>
    <th>Location</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @events.each do |event| %>
  <tr>
    <td><%= event.title %></td>
    <td><%= event.date %></td>
    <td><%= event.time %></td>
    <td><%= event.venue %></td>
    <td><%= event.location %></td>
    <td><%= link_to 'Show', event %></td>
    <td><%= link_to 'Edit', edit_event_path(event) %></td>
    <td><%= button_to 'Delete', { :action => "destroy", :id => event.id }, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Create New Event', new_event_path %> | 
<%= link_to "View Events Page", events_events_path %>
